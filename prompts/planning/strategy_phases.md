# Phase Decomposition Strategy (Root Level)

## 拆解方針
1. **宏觀架構**: 將全局目標拆解為 2-4 個具備時序性的「相位 (Phase)」。
2. **節點屬性**: 
   - `type`: 必須標註為 `phase`。
3. **純粹拆解**: 此階段僅定義各相位的 `goal` 與 `description`，嚴禁定義具體的交付物或驗證標準。
4. **數量控制**: 嚴禁超過 5 個節點。
